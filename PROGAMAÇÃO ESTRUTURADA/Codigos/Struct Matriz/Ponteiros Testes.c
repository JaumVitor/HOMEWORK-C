#include <stdio.h> 

int main (){
	int x; 
	int y; 
	x=10; 
	y=200; 
/*	
// >>> Este ponteiro vai apontar para um endereço do tipo inteiro; (Neste momento ele não sabe para onde vai apontar)
	int *pont;
//>>> Aqui ele vai receber o endereço....pra onde *pont vai apontar;  
	pont = &y; //Pont esta recebendo o endereço, para onde vai apontar; 
	pont = y; //Recebendo o valor armazenado na memoria; 

	printf ("%d\n", *pont); 
*/ 
/*
	 x = 100;
	 int *ponteiro; 
	 ponteiro = &x; 
	 
	 int *pont2; 
	 pont2 = &y; 
	  
	 y = 250;
	 *ponteiro = y;  
	 
	 printf ("%d-%d\n", x, y);
	 printf ("%d", *pont2); 	
*/
	 
return 0; 
}